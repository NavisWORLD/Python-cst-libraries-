# CST Portable Protocols

`CST-SYNAPTIC-V1` is the first language-neutral numerical contract.

- `synaptic-v1.schema.json` describes JSON-bridge requests.
- `conformance.json` contains cross-language reference values.

Protocol additions should be backward-compatible or use a new protocol ID.
