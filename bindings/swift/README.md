# Swift

Compile with `swiftc bindings/swift/Synaptic.swift bindings/swift/main.swift -o /tmp/cst-swift` and run `/tmp/cst-swift`. Apple applications may alternatively call the C ABI through a bridging header or module map.
