# Kotlin

Kotlin projects can call the Java implementation in `bindings/java/Synaptic.java` directly because both run on the JVM. `Synaptic.kt` is also included as an idiomatic Kotlin reference implementation.
