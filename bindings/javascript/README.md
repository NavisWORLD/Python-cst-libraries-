# JavaScript / TypeScript

The binding is a zero-dependency ES module with TypeScript declarations. Run `node bindings/javascript/test.mjs`. Browser bundlers can import `synaptic.js` directly and TypeScript consumes `synaptic.d.ts`.
