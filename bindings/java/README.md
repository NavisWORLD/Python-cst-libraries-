# Java / JVM

Compile with `javac -d /tmp/cst-java bindings/java/Synaptic.java bindings/java/SynapticTest.java` and run `java -cp /tmp/cst-java SynapticTest`. Kotlin, Scala and Clojure can call `org.cosmos.cst.Synaptic` directly.
